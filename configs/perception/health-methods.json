{
  "schema_version": "horizon.perception-health-methods.v1",
  "status_semantics": {
    "healthy": "declared checks passed inside frozen calibrated scope",
    "degraded": "one or more calibrated checks exceeded its alarm condition",
    "invalid": "perception evidence was numerically or structurally invalid",
    "unknown": "required evidence, artifact, or calibrated scope is unavailable"
  },
  "methods": [
    {"id": "H0", "entrypoint": "horizon_neural_health.entrypoints:h0", "reference_required": false},
    {"id": "H1", "entrypoint": "horizon_neural_health.entrypoints:h1", "reference_required": false},
    {"id": "H2", "entrypoint": "horizon_neural_health.entrypoints:h2", "reference_required": true},
    {"id": "H3", "entrypoint": "horizon_neural_health.entrypoints:h3", "reference_required": true},
    {"id": "H4", "entrypoint": "horizon_neural_health.entrypoints:h4", "reference_required": true, "offline_intervention_validation_required": true}
  ],
  "runtime_boundary": "health records may select a frozen uncertainty/mode policy; they never emit steering or metric obstacle bounds"
}
